const Problem9 = () => {
  const MaxNumber = (numbers) => {
    return Math.max(...numbers);
  };

  const numbers = [2, 10, 15, 50, 60];

  console.log(MaxNumber(numbers));

  return <div></div>;
};

export default Problem9;
